public String evenOrOdd(Integer num) {
   if (num == null) {
        return 'Invalid input';
    }
    if (Math.mod(Math.abs(num), 2) == 0) {
        return 'even';
    } else {
        return 'odd';
    }
}