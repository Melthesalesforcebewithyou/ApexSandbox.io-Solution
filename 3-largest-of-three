public static Integer findLargest(Integer num1, Integer num2, Integer num3) {
	//Use Math.max method to indentify largest of three integers
    //This method can only compare 2 at a time
    Integer largest = Math.max(num1, num2);
    largest = Math.max(largest, num3);
    //Return the Ingeter with the largest number
    return largest;
}