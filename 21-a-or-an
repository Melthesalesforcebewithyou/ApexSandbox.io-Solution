public String aOrAn(String word) {
    String a = 'a ';
    String an = 'an ';
    //Check if String parameter is null or empty 
    if (word == null || word.isEmpty()) {
        return null;
    }
    //Check if the word starts with a vowel or not
    //Change to lowerCase

    String lowerWord = word.toLowerCase();

    if (lowerWord.startsWith('a') || lowerWord.startsWith('e') || lowerWord.startsWith('i') || lowerWord.startsWith('o') || lowerWord.startsWith('u')) {
        return an + word;
    } else {
        return a + word; 
    }
}